[flake8]
exclude =
     # No need to traverse our git directory
    .git,
    # There's no value in checking cache directories
    __pycache__,
    # This is a submodule
    Beatporter,
    matrix.py
    kivy_tut.py
statistics = False
max-line-length = 120